import{_ as t,c as r,a0 as i,o as a}from"./chunks/framework.p2VkXzrt.js";const l=JSON.parse('{"title":"Helper Functions Overview","description":"","frontmatter":{},"headers":[],"relativePath":"utilities/helper-functions.md","filePath":"utilities/helper-functions.md"}'),s={name:"utilities/helper-functions.md"};function o(n,e,d,c,u,h){return a(),r("div",null,e[0]||(e[0]=[i('<h1 id="helper-functions-overview" tabindex="-1">Helper Functions Overview <a class="header-anchor" href="#helper-functions-overview" aria-label="Permalink to &quot;Helper Functions Overview&quot;">​</a></h1><p>The following helper functions are commonly used for general utility tasks like handling errors, sanitizing input, managing CSRF protection, redirecting pages, and debugging. These functions enhance the security and usability of your project by providing reusable methods for various operations.</p><h2 id="_1-sanitizeinput-data" tabindex="-1">1. <code>sanitizeInput($data)</code> <a class="header-anchor" href="#_1-sanitizeinput-data" aria-label="Permalink to &quot;1. `sanitizeInput($data)`&quot;">​</a></h2><p>This function is used to clean user input to prevent security issues such as XSS (Cross-Site Scripting) attacks and other vulnerabilities.</p><p>Purpose: Sanitizes and escapes input data by stripping tags and converting special characters to HTML entities.</p><p>Usage: It processes both strings and arrays recursively, making sure any input is safe before being used in the application.</p><p>How it works:</p><ul><li>strip_tags(): Removes HTML and PHP tags.</li><li>trim(): Removes extra spaces from the input.</li><li>htmlspecialchars(): Converts special characters to HTML entities (to prevent code execution).</li><li>ENT_QUOTES: Ensures both double and single quotes are converted.</li></ul><h2 id="_2-csrf" tabindex="-1">2. <code>csrf()</code> <a class="header-anchor" href="#_2-csrf" aria-label="Permalink to &quot;2. `csrf()`&quot;">​</a></h2><p>Generates and inserts a hidden CSRF (Cross-Site Request Forgery) token into an HTML form. This token helps to verify that the requests made are from the correct user, preventing CSRF attacks.</p><p>Purpose: Protects the application from CSRF by generating a token and embedding it into forms.</p><p>Usage: Call this function in your form to include the CSRF token.</p><p>How it works:</p><p>If a csrf_token doesn&#39;t exist in the session, a new one is created using bin2hex(random_bytes(32)) to generate a secure, random token. The token is then rendered as a hidden input field in your form.</p><h2 id="_3-validatecsrftoken-token" tabindex="-1">3. <code>validateCsrfToken($token)</code> <a class="header-anchor" href="#_3-validatecsrftoken-token" aria-label="Permalink to &quot;3. `validateCsrfToken($token)`&quot;">​</a></h2><p>Validates the CSRF token submitted with a form request to ensure it matches the token stored in the session.</p><p>Purpose: Verifies that the CSRF token in the request is valid and prevents unauthorized or malicious form submissions.</p><p>Usage: Call this function with the token submitted via a form to check if it matches the one stored in the session.</p><p>How it works:</p><p>It compares the session token with the provided token using hash_equals(), which prevents timing attacks by securely comparing strings.</p><h2 id="_4-register-shutdown-function-with-error-handling" tabindex="-1">4. <code>register_shutdown_function() with error handling</code> <a class="header-anchor" href="#_4-register-shutdown-function-with-error-handling" aria-label="Permalink to &quot;4. `register_shutdown_function() with error handling`&quot;">​</a></h2><p>This function is used to catch fatal errors and redirect the user to a custom error page.</p><p>Purpose: Handles fatal errors that might occur at runtime and logs the error details. It also redirects the user to a custom error page.</p><p>Usage: It is automatically invoked when the script ends, ensuring that if a fatal error occurs, it will be logged and the user will be redirected to a specified error page.</p><p>How it works:</p><p>error_get_last(): Retrieves the last error that occurred. If the error is fatal (like E_ERROR), it logs the error and redirects the user to an error.html page with the error message as a query parameter.</p><h2 id="_5-redirect-url-message" tabindex="-1">5. <code>redirect($url, $message = &quot;&quot;)</code> <a class="header-anchor" href="#_5-redirect-url-message" aria-label="Permalink to &quot;5. `redirect($url, $message = &quot;&quot;)`&quot;">​</a></h2><p>Redirects the user to a specified URL and optionally passes a message to be stored in the session.</p><p>Purpose: Simplifies the process of redirecting users while passing messages (e.g., success or error messages).</p><p>Usage: Call this function after performing actions like form submission or authentication to redirect the user to another page.</p><p>How it works:</p><p>If a message is provided, it stores the message in the session under $_SESSION[&#39;message&#39;]. After that, it uses the header(&quot;Location:&quot;) function to redirect the user to the specified URL.</p><h2 id="_6-dd-arr" tabindex="-1">6. <code>dd($arr)</code> <a class="header-anchor" href="#_6-dd-arr" aria-label="Permalink to &quot;6. `dd($arr)`&quot;">​</a></h2><p>This function is commonly used for debugging purposes. It dumps an array (or any variable) in a readable format and halts further script execution.</p><p>Purpose: To inspect the contents of a variable or array during development.</p><p>Usage: Call dd() to dump the contents of any variable and immediately stop the script.</p><p>How it works:</p><ul><li><code>print_r():</code> Outputs the contents of the array or variable in a human-readable format.</li><li><code>die():</code> Stops the execution of the script, which is helpful for debugging because it ensures that no further code is executed after the output.</li></ul>',38)]))}const f=t(s,[["render",o]]);export{l as __pageData,f as default};
